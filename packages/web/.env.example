# URL of the embedded eXeLearning static editor used in the post-conversion
# preview panel. The editor must implement the postMessage embedding protocol
# (EXELEARNING_READY / OPEN_FILE). Defaults to https://static.exelearning.dev/.
VITE_EXE_EDITOR_URL=https://static.exelearning.dev/
